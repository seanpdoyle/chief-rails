#!/bin/sh
#
# Deploy to the provided environment

set -e

if [ -z "$1" ]; then
  red "you have must provide an environment"
  return $FAIL
fi

BRANCH=$(git rev-parse --abbrev-ref HEAD)

git push $1 $BRANCH:master

heroku run RACK_ENV=$1 rake db:migrate paperclip:refresh:missing_styles --remote $1