{
  "type": "object",
  "required": ["spots", "images"],
  "properties": {
    "spots" : {
      "type" : "array",
      "items": <%= schema_for("spot") %>
    },
    "images" : {
      "type" : "array",
      "items": <%= schema_for("image") %>
    }
  }
}
